# Lost Scrolls II

(Project/codebase formerly named "Dvergr Expanded" — renamed to reflect that this is explicitly the sequel, not a spinoff. The working directory folder has now also been renamed to match: `E:\Valheim Modding\Lost Scrolls II`. Published to GitHub at `https://github.com/yesu0725/Lost-Scrolls-II`.)

A spiritual sequel to the deprecated Thunderstore mod **Lost Scrolls** (TaegukGaming). This is not a restoration of the old mod's content — it's a continuation of its lore, rebuilt as a new vanilla-assets-only Valheim mod centered on recruitable, levelable Dvergr companions.

## Concept

Lost Scrolls told the story of Damon and his "reign of darkness," fought through stolen scrolls scattered across the world. Lost Scrolls II picks up the thread: Damon's curse never fully died — it lingers as the corruption that holds the last Dvergr in their twisted state. The scrolls in this mod are fragments of the Dvergr's own **Communion Rite**, capable of freeing a corrupted Dvergr and turning it into an ally instead of just defeating it.

Damon himself does not return as a character — see [docs/Lore.md](docs/Lore.md) for why and how his influence is handled instead.

## Core Features

- **Ally recruitment** — perform the Communion Rite on a Dvergr to free and recruit it. See [docs/Ally-Recruitment.md](docs/Ally-Recruitment.md).
- **Ally leveling** — recruited Dvergr gain XP and grow stronger over time. See [docs/Ally-Leveling.md](docs/Ally-Leveling.md).
- **Ally chores** — assign allies to vanilla workstations to automate tedious tasks. See [docs/Ally-Chores.md](docs/Ally-Chores.md).
- **Dvergr duels** — put your companion into a non-lethal "duel mode" to spar against **another player's** duel-mode companion. See [docs/Duel-Arena.md](docs/Duel-Arena.md).
- **Ally commands** — feed/heal a companion, and cycle its stance between Follow/Guard/Standby. See [docs/Ally-Commands.md](docs/Ally-Commands.md).
- **Story delivery** — lore and quest content are told through the sibling mod **Valheim ServerGuide**, not built into this mod. See [docs/ServerGuide-Integration.md](docs/ServerGuide-Integration.md).

## Hard Constraint: Vanilla Assets Only

This mod uses **only** vanilla Valheim assets and prefabs — no custom models, textures, sounds, or AssetBundles. Every recruitable Dvergr type is an existing vanilla creature with new behavior attached, not a new creature. See [docs/Technical-Constraints.md](docs/Technical-Constraints.md) before implementing anything visual or asset-related.

## Related Project

`E:\Valheim Modding\Valheim ServerGuide` is a separate, already-functional BepInEx mod that delivers narrative content via trigger-driven `guidance.yaml` chains (raven popups, messages, rune/intro text). Lost Scrolls II fires events into it rather than building its own quest-text UI. See [docs/ServerGuide-Integration.md](docs/ServerGuide-Integration.md).

## Documentation Index

| Doc | Covers |
|---|---|
| [docs/Lore.md](docs/Lore.md) | Story, the gospel allegory, the biome descent + mirror, the four Dvergr castes, location-to-story mapping |
| [docs/Quest-Script.md](docs/Quest-Script.md) | **Historical** — the earlier scene-by-scene *act*-structured draft (superseded by the biome descent) |
| [docs/Quest-Chains-Draft.yaml](docs/Quest-Chains-Draft.yaml) | **Historical** — conceptual `guidance.yaml` chain draft (Phase 1; superseded) |
| [docs/Ally-Recruitment.md](docs/Ally-Recruitment.md) | Communion Rite mechanics, recruit conditions |
| [docs/Ally-Leveling.md](docs/Ally-Leveling.md) | XP, stat growth, level-up events |
| [docs/Ally-Chores.md](docs/Ally-Chores.md) | Workstation automation AI |
| [docs/Duel-Arena.md](docs/Duel-Arena.md) | Non-lethal sparring system |
| [docs/Ranking.md](docs/Ranking.md) | **Built (Phase A/B, unverified)** — persistent server-authoritative 1v1 duel ladder (Elo, JSON store, seasons, `dvergr_rank_changed`) |
| [docs/Party-Duels.md](docs/Party-Duels.md) | **Party duels + ranking built (Phase C/D, unverified)** — team-vs-team + owner-keyed record with companion snapshot |
| [docs/Tournaments.md](docs/Tournaments.md) | **Built (Phase E, unverified)** — bracket runner for 1v1 & party, ServerGuide-delivered prizes, Hall of Champions |
| [docs/Tournament-Announcement.md](docs/Tournament-Announcement.md) | Player-facing announcement block + mechanics summary for the tournament system (copy-paste for Discord/server posts) |
| [docs/Bounty-Hunting.md](docs/Bounty-Hunting.md) | **COMPLETE (A–H verified, released 0.8.0)** — server-only, triple-gated (BiomeLords + ServerGuide + Valheim Donations) bounty hunting: auto-aggressive scaled Dvergr targets, ServerGuide-authored item rewards + rank-gated Valcoin chance (reward-only), dedicated leaderboard/UI, NPC-dialogue quest gate |
| [docs/Wagers.md](docs/Wagers.md) | **NEW (unverified)** — player-started staked tournaments (one Coin + one Valcoin slot, fixed 4-player, ready-up venue choice) and staked duel invites; the Valcoin debit bridge into Valheim Donations; direct-to-webhook Discord announcements |
| [docs/Ally-Commands.md](docs/Ally-Commands.md) | Feed/heal, Follow/Guard/Standby stance, rename, hover tooltip, minimap pins, portal follow |
| [docs/Ally-Inventory.md](docs/Ally-Inventory.md) | Per-companion 4×2 inventory, chest-like UI + rename field, item pickup, food/mead consumption, weight cap/encumbrance |
| [docs/Companion-Totems.md](docs/Companion-Totems.md) | Seal a companion into a GoblinTotem at the Incinerator (Wisps, 1:1) and summon it back |
| [docs/ServerGuide-Integration.md](docs/ServerGuide-Integration.md) | New trigger types, integration contract |
| [docs/Technical-Constraints.md](docs/Technical-Constraints.md) | Vanilla-assets-only rules, prefab reuse approach |
| [docs/Development-Phases.md](docs/Development-Phases.md) | Phase-by-phase build plan |
| [docs/Testing.md](docs/Testing.md) | In-game test checklist for every mechanic (persistent reference) |
| [docs/Publishing.md](docs/Publishing.md) | GitHub repo, wiki, and the single Thunderstore package — rebuild/upload steps (the Quest pack was retired at 0.10.0) |
| [guidance/](guidance/) | The nine ServerGuide story/reward YAMLs + install README — the distribution point since the Quest pack was retired |
| [docs/Ship-Riding.md](docs/Ship-Riding.md) | Follow-stance companions board via ladder, then walk the deck freely (vanilla AI carries them along) |
| [docs/Changelog.md](docs/Changelog.md) | Consolidated record of feature batches and fixes, newest first |

## Status

All core mechanics phases (0–6) now have code in place: scaffold, lore/quest script, recruit, leveling, chores, ServerGuide integration, and non-lethal duels — plus a post-Phase-6 feature batch (hover indicators, Follow/Guard/Standby stance) and a project rename from "Dvergr Expanded" to "Lost Scrolls II" (GUID `com.lostscrollsii`, namespace `LostScrollsII`). Two feedback passes have since fixed several bugs and reworked systems: the recruited-Dvergr-still-attacks bug (root cause was the AI's `m_aggravated` flag, not faction); recruit state reverting to uncommuned after a relog (now rebuilt on spawn from the `DE_Recruited` ZDO flag via a `MonsterAI.Start` patch); feeding (now mead-based on the `G` key, detected by the potion's consume **status effect** since meads carry no `m_food`, healing a flat amount and playing the mead's own VFX); and leveling (cap raised to **10** with a rising XP curve, **biome-/HP-scaled** kill XP read live at kill time, player-kill XP, and a custom gold **`★N` badge** replacing vanilla stars — which only render 2). **None of this has been verified in a live game session** — see [docs/Testing.md](docs/Testing.md) for the full test checklist. Highest-risk unverified items: the reworked duels now need **two players** to test (cross-client `DE_Duel` replication, non-lethal, PvP-immunity, auto-stand-down); the 7 per-biome reference-HP values for XP scaling are documented, not extracted (the `[xp]` log prints live HP so they can be corrected). *(The old Phase-6 `ApplyDamage`-prefix risk is gone — non-lethal now rides on the Cecil-confirmed `Character.Damage(HitData)` prefix.)* Other open items: stance is in-memory only; Phase 6 has no arena structure (by design). **Chore assignment now persists** across relog/zone reload (chore kind + target **world position** on the companion ZDO — *not* ZDOID, which doesn't survive the connection/remap system; re-resolved by proximity on spawn) and keeps running while the owner is away — gated to the ZDO owner, with the Valheim caveat that fully-unloaded zones don't simulate (it pauses, then resumes on reload). A reported `CookingStation.IsFireLit` NRE on the **Stone Oven** was fixed by gating the fire check behind `m_requireFire` (ovens are their own heat source and leave fire-check points unconfigured). A later batch added: caste-gated chores (Fire→smelting, Ice→refining, Support→provisioning/farm/husbandry, Rogue→haul) with item-specific voiced blockers, fuel-feeding, vertical reach, vanilla add-VFX, and chore/standby passivity; per-player ownership + selective threat (owner-only commands, Guard-treats-others-as-threats, retaliation); reduced jump height; chore-assign hover tooltips; and the `de_spawn` admin command. **Most recent batch (unbuilt items from the menu request):** the chore-selection menu was **discarded** in favor of a **claim registry + hover tooltip** ("&lt;name&gt; is already working here", no double-assign); **haul** stays a radius-sweep (companion holds its post by the chest, pulls items in range straight in, lid opens on deposit — the walk-to-item/pickup-VFX version was tried then reverted at the user's request); **vanilla Dvergr chatter** (`NpcTalk`) is disabled on recruit and replaced by stance/caste **capability lines**; the stance **menu was decided against** (kept as a cycle). All of this builds clean but is **unverified in a live session**. **Latest tweaks:** the stance-cycle key moved **`F` → `E`**; companions can be **renamed** (key **`Y`**, vanilla `TextInput`/`TextReceiver`, stored on ZDO `DE_Name`, reflected in the floating name+badge, the crosshair tooltip, and the chore claim tooltip); and a **crosshair hover tooltip** on owned companions shows current stance + `[E] Cycle stance` / `[Y] Rename` (via a `Character.GetHoverText` postfix). All our hotkeys are suppressed while a text field/chat/console has focus. **Duel rework (2026-07-02):** the scripted 1v1 `DuelController` (+ its `ApplyDamage` prefix) was **deleted** in favor of a companion **"duel mode"** — the owner toggles it on their **own** companion (`J`); it then fights **only** other players' duel-mode companions (different, non-zero owner), ignores/‌is-immune-to everyone else, and auto-stands-down when no rival remains, on a wait timeout, or when the owner logs out/leaves ~40m — **making duels multiplayer-only** (no solo XP-farming). Targeting rides on the rewritten `BaseAI.IsEnemy` patch; non-lethal (5% floor) + winner +50 XP + PvP immunity fold into the `Character.Damage(HitData)` prefix; state is ZDO-backed (`DE_Duel`) and cleared on spawn. Same batch: an **owner name tag** on companion floating names (persisted ZDO `DE_OwnerName`), and a **butcher-knife betrayal** — striking a (non-dueling) companion with a `KnifeButcher` turns it feral against all players incl. the owner (`GoFeral`). Builds clean; duel path unverified pending a two-player session. **Lore/quest finalization (2026-07-02):** the story chains in `guidance.lost-scrolls.yaml` had their placeholder identifiers resolved to confirmed vanilla ids (scroll fragments → `SurtlingCore` / `WitheredBone`, Sword of Truth → `SwordMistwalker`, the Act 3 "first corrupted Dvergr" beat → `location_entered` on `Mistlands_DvergrTownEntrance*` [changed from a `kill` trigger to fit the free-don't-kill theme], Act 4.3 stronghold → `location_entered` on `Mistlands_DvergrBossEntrance1` [was an unsupported `location` type that would never fire]); the four per-caste recruit **voices** were polished from DRAFT to final text and de-duplicated against the act beats by display channel (act beats speak in the world's voice `intro`/`rune`, always-on voices in the raven's `raven` — the Act 4 Fire/Ice beats moved raven→rune to keep the channels distinct); and the **finale is kept deliberately open** — the Act 6 epilogue stays ambiguous (may-or-may-not-be-a-boss, no named villain), with the actual plan for a future major update (a finale boss + an "Armor of God" gear set) held only in [docs/Lore.md](docs/Lore.md)'s **author-only roadmap note** and kept out of all in-game and public-facing text so it can't spoil the update. The one remaining lore/quest open item is live in-game verification of the chains (the YAML still needs merging into an active `guidance.yaml`). **Companion totems (2026-07-02):** a new system to **seal a recruited companion into a carriable `GoblinTotem`** and **summon it back** — vanilla assets only. Sealing is an **Incinerator** ritual: gather **Follow**-stance companions at an Obliterator, drop in **Wisps** (1:1), pull the lever; the swap resolves during the vanilla 5–7 s lighting animation (`Incinerator.OnIncinerate` prefix takeover) into **named** totems (`N = min(wisps, followers)`, surplus left untouched). Per-companion **level/XP/name/owner** ride on the item's persisted `ItemData.m_customData` (+ a `GetTooltip` postfix for the stat block). The totem is renamed **"Communion Totem"** with a purpose-based description via a **per-instance `SharedData` clone** (real Fuling Totems keep vanilla name/stacking; the clone carries `m_maxStackSize = 1` so companion totems never merge — re-applied on load via two `ItemDrop.LoadFromZDO` postfixes). **Summoning** uses the totem from the hotbar/inventory (`Humanoid.UseItem` prefix) to respawn the companion **where the player is looking** via `CommunionService.SpawnRecruited(... , xp)`. **VFX** reuse vanilla effect prefabs (soul-dissipation on seal, spawn burst on summon; resolved from `ZNetScene` with graceful fallback). Builds clean; **unverified in a live session** (see [docs/Testing.md](docs/Testing.md) §12) — MP path (cross-client incinerator ownership) needs a two-client pass. **Ship riding (2026-07-02):** Follow-stance companions now **get aboard the owner's ship** — a new `ShipRideAI` (attached alongside `DvergrCompanion`, owner-ZDO gated, transient) trails to the hull via normal Follow and **boards through a `Ladder`** (snaps up to `Ladder.m_targetPos`; laderless rafts board alongside), speaking a one-time *"Aboard…"* line. Once standing on the ship it's **left alone — free to walk the deck**: vanilla `MonsterAI` keeps following the owner and the ship's platform physics carries it, so it fights and moves normally on deck (**no seats, no position-locking, no idle suppression** — an earlier build that snapped companions into vanilla `Chair` seats and pinned them was removed per request). If it walks off into the water beside the boat while the owner is still aboard it's **lifted back on**. Companions also **avoid water by default** (`BaseAI.m_avoidWater = true`, baseline in `ApplyFreedState`) — `ShipRideAI` clears it **only while the owner is aboard a ship**, so an ally swims out to board but won't otherwise wander into the sea. Vanilla assets only (`Ship`/`Ladder` + platform physics; no new prefabs). Builds clean; **unverified in a live session** (see [docs/Testing.md](docs/Testing.md) §13 + [docs/Ship-Riding.md](docs/Ship-Riding.md)). **Companion minimap pins (2026-07-02):** a new client-side `CompanionMapPins` (component on the plugin GameObject) shows a **live minimap pin at each of the local player's own companions** — one transient (`save = false`) vanilla `Minimap.AddPin` per owned companion, refreshed ~4×/s to its position, label = display name, removed on despawn. Because pins are client-side and only companions whose `DE_Owner` matches the local player are pinned, **other players never see your companions on their map** (and you never see theirs); unowned/legacy allies aren't pinned. Config (`Companions`): `ShowMapPins` (default on), and (as of 2026-07-13) the pins use the **player icon** tinted+scaled — `CompanionPinColor` (hex, default `FFB84D`) + `CompanionPinScale` (default 0.7); plus a per-companion **death marker** (skull pin named for the ally, dropped on the owner's map on death, config `ShowDeathMarker`). *(The old `MapPinIcon` 0-4 config was removed.)* Added a `Splatform.dll` reference for the `PlatformUserID` arg. Builds clean; **unverified in a live session** (see [docs/Testing.md](docs/Testing.md) §14). **Chore recall on companion (2026-07-02):** pressing the chore key **`H` while hovering your own companion** now unassigns its chore directly ("Ally returns to your side") — no need to re-hover the station; handled at the top of `Plugin.HandleChoreAssignInput`, with a `[H] Recall from chore` crosshair-tooltip hint shown while it's assigned. **Portal follow (2026-07-02):** Follow-stance companions now **teleport through a portal with the owner** — a `CompanionPortalPatch` postfix on `TeleportWorld.Teleport(Player)` reads the destination the portal recorded on the player (`m_teleportTargetPos/Rot`) and moves every companion that's **in Follow stance, owned by that player, and loaded nearby** to the exit (spread in a small ring), committing each companion's ZDO position (after `ClaimOwnership`) so it survives the zone change and re-instantiates at the destination. Only requirement is Follow — chore/duel/feral/Guard/Standby allies stay put. Vanilla assets only; builds clean; **unverified in a live session** (see [docs/Testing.md](docs/Testing.md) §15). *(The earlier `LadderClimbAI` ladder-climbing experiment was **removed** — it didn't reliably detect/route to `wood_stepladder`; portals cover the "get the ally to me" need instead.)* **§12–§15 now verified in-game (2026-07-03):** companion totems, ship riding, minimap pins, and portal follow all **passed** a live session. **PvP/duel batch (2026-07-03):** fixed the "**duel loser can't be healed by mead**" bug — `Character.SetHealth` only writes on the ZDO owner (no RPC fallback), and the cross-client subdue (attacker-side `Character.Damage` prefix → `ExitDuelMode` → `ClaimOwnership`) leaves the loser's ZDO owned by the **winner's** client, so the owner's feed wrote into the void; `MeadFeedingService.TryFeed` now heals via **`Character.Heal`** (routes to the ZDO owner over `RPC_Heal`, no ownership stealing). Same change makes **feeding not owner-gated** — any player can feed any companion (top up a friend's ally or heal a duel loser); stance/rename/chore/duel stay owner-only. Plus four PvP behaviors: (1) a player-struck companion also turns on the **attacker's companions** (`MarkOwnersCompanionsHostile`), not just the player; (2) when a player attacks another player with **both PvP on**, the **attacked** player's companions turn on the aggressor **and its companions**; (3) a duel win is broadcast as a chat **shout** (`Talker.Type.Shout`); (4) a **`[J]` duel hint** shows on your companion when another player's companion is in range. Enabler: `DvergrCompanion.IsHostileTo` generalized from `Player` to any `Character` so companion-vs-companion hostility flows through the timed dictionary + `BaseAI.IsEnemy` patch. Builds clean; the four PvP/duel items are **unverified — need a two-player session** (see [docs/Testing.md](docs/Testing.md) §7c/§9). **§7c & §9 later verified in-game (2026-07-03):** ownership/threat and the full duel path (incl. the four PvP/duel items) **passed** a two-player session. **Story/guidance overhaul (2026-07-03):** the ServerGuide narrative was **reworked from 6 "Acts" into a biome-by-biome descent** — reflective beats fire at distinct world locations Meadows→Ashlands via the **`distance`** trigger (chosen over `location_entered` because the latter's one-shot per-location dedup, burned for *every* place a player nears, would permanently block the beats for anyone already on the server; `distance` only burns when a matching beat is in range, so it fires for veterans too). Theme: a mirror held up to the player (the toiling Dvergr are us — slaves of our own path, in unrecognized rebellion; the chore system is the thesis made playable), with **Scripture woven in verbatim but never cited**; `intro` mode only at the opening (`StartTemple`) and the Ashlands finale, `rune`/`raven` between. The lore **begins at `StartTemple`**; established players are nudged back by `ls_call_to_start` (a `timed` raven, `stop_when` the opening). Beats are **independent entries** (chains would stall on already-passed locations), ordered by directional text. Added a plain **recruit-order guide** (`ls_guide_recruit_order`, tracked chain: Rogue→Fire→Ice→Support) and a separate **Companion Handbook** (`guidance.companions.yaml`, `category: Companions`) teaching the command keys, per-caste chores, and adventure use (ships/portals/totems/leveling) via `dvergr_recruited`/`ship_sailed`/`portal_used`/`dvergr_level_up`/`build` triggers. All authored content is **deployed to the test profile** (ServerGuide auto-merges every `*.yaml`); `docs/Lore.md` updated to the descent structure. **Verified in-game (2026-07-03):** §10b (biome descent incl. the StartTemple opening + returning-player nudge), §10c (recruit-order guide), and §10d (Companion Handbook) all **passed** — the `distance`-over-`location_entered` choice and the `Charred*`/`incinerator` names held up. See [docs/Testing.md](docs/Testing.md) for the full test checklist and [docs/Development-Phases.md](docs/Development-Phases.md) for the roadmap. **Publish/packaging (2026-07-03):** the folder was renamed `Dvergr Expanded` → `Lost Scrolls II`, the project pushed to GitHub (public) at `https://github.com/yesu0725/Lost-Scrolls-II` with a spoiler-free player wiki (in-repo `wiki/` + the GitHub Wiki), and **two Thunderstore packages** were built under `Thunderstore files/` — the **base** `Lost Scrolls II` (gameplay only, DLL + 256×256 `icon.png`, BepInEx dep only, ServerGuide optional) and the **`Lost-Scrolls-II-Quest`** complete pack (no bundled DLL; bundles the story/handbook YAMLs under `config/ValheimServerGuide/` and hard-depends on `TaegukGaming-Lost_Scrolls_II-0.1.0` + `TaegukGaming-ValheimServerGuide-0.7.1` + BepInEx). Upload the base package first (Thunderstore validates the Quest pack's base-mod dependency at publish time). Full detail in [docs/Publishing.md](docs/Publishing.md). **Companion inventory (2026-07-05):** each recruited Dvergr now carries its **own 4×2 (8-slot) inventory**, built on a vanilla `Container` put on the creature (sharing its `ZNetView`) so ZDO persistence, cross-client sync and a **chest-identical `InventoryGui.Show` panel** (player inventory + crafting + total-weight readout) all come for free. Opening moved onto the **`Y` key** (was rename) — `Y` now opens the pack **and** carries a **rename field** (a clone of the vanilla `TextInput` `GuiInputField` reparented into the container panel); `CompanionContainerAccessPatch` suppresses the vanilla `[E] Open` hover/interact so the bag stays owner-gated. `CompanionInventoryAI` (owner-ZDO-gated 1 Hz tick, attached automatically by `CompanionInventory`) adds: **pickup** (radius-sweep of matching loose `ItemDrop`s — only item **types it already holds**, empty pack picks up nothing, **combat/chore/duel suspends** gathering); **food** (eats **one at a time**, temporary **max-HP buff** = the food's `m_food` for its burn time, delta-based decay, **fed icon** = the food's own sprite); **health mead** (sip below **35%** HP, stop above **90%**); **resist meads** (poison/fire/frost — apply the same `StatusEffect` the player gets and keep drinking while any remain; classified by the consume effect's **resistance modifier**, so stamina/health meads are excluded — `CompanionConsumables`); and a **150 weight cap** (over it: no pickup, **won't attack** — target dropped each tick — but still **moves**, plus a vanilla **Encumbered** status icon). Status icons render as real game sprites injected into the `EnemyHud` element (`CompanionStatusIconPatch`). **Verified in-game across three feedback passes (2026-07-05)** — see [docs/Ally-Inventory.md](docs/Ally-Inventory.md) + [docs/Testing.md](docs/Testing.md) §16. The passes added/fixed: the resist readout was **removed from the pack panel** (shown above the companion only); an **`HP cur/max`** readout by the name confirms the food buff; the name field moved out from under the title; **encumbrance now truly stops attacks** (enforced every frame via `DvergrCompanion.ApplyEncumbrance`); the **health mead sipping is latched** across the full 35%→90% window; the companion **drops its whole pack on death** (`CompanionDeathDropPatch`); a **Communion Totem carries the sealed pack** through seal→summon (`DE_TotemInv` on the item's `m_customData`); the **`portal_wood` blocks a player** whose Follow companion carries a non-teleportable item (`CompanionPortalBlockPatch`); **typing the name suppresses all binds** (a `ZInput.GetButtonDown` prefix — the earlier gate only stopped our own keys, so `E` still closed the panel); and a **ComfyQuickSlots** panel-gap fix pushes `InventoryGui.m_container` down by the extra player-inventory rows while the pack is open (BiomeLords' technique), restored on close. **Competitive-features design + double-subdue fix (2026-07-08):** planned a four-part competitive suite — **1v1 duel ranking**, **party duels**, **party ranking**, and a **tournament system** — all server-authoritative, persisted to a **JSON file next to the world save** (keyed by world name), synced to clients over a new `LeaderboardSync` RPC (mirroring ServerGuide's `GuidanceSync`), and delivered/​rewarded entirely through ServerGuide (new triggers `dvergr_rank_changed`, `dvergr_party_duel_won`, `dvergr_party_rank_changed`, `dvergr_tournament_joined`/`_match`/`_won`; prizes via the existing `RewardDispatcher`). Records key on a new **stable `DE_CompanionId` GUID** (assigned at recruit, carried through the Communion Totem's `m_customData` — ZDOIDs don't survive seal/summon/relog) denormalized with owner id/name; rating is **Elo** (start 1000) with a per-pair cooldown against friend-farming; **seasons** archive + reset the board. Decisions locked: JSON store (not ServerGuide global keys), and build **Phase A (foundations: `DE_CompanionId`, `LeaderboardStore`, `LeaderboardSync`, Elo, new bridge triggers) + Phase B (1v1 ladder) end-to-end and verify in a 2-player session before** committing party (Phase C/D) and tournament (Phase E) code. Full plans in [docs/Ranking.md](docs/Ranking.md), [docs/Party-Duels.md](docs/Party-Duels.md), [docs/Tournaments.md](docs/Tournaments.md). **Prerequisite bug fixed first:** the duel **double-win** bug — capping a loser's HP at the 5% floor left it on the threshold, vanilla `Character`/`MonsterAI` regen ticked it back above the floor within a frame or two while the winner was still swinging and before `ExitDuelMode` replicated, so a second hit re-fired the win (double announcement + would double-count points). Fixed with an **in-memory idempotency latch** (`DvergrCompanion._duelResolved`): the subdue now routes through the idempotent `ResolveSubdue(winner)` (resolves the bout exactly once, reset only on re-entering duel mode) and the damage prefix swallows further hits on an already-resolved duelist (`IsDuelResolved`). Party duels will reuse the same latch to bench a member once. Builds clean; the latch needs a 2-player duel re-verification (see [docs/Duel-Arena.md](docs/Duel-Arena.md)). **Competitive suite — Phase A + B built (2026-07-09):** the 1v1 duel ladder is implemented end-to-end and builds clean (both projects). Foundations: a stable **`DE_CompanionId`** GUID assigned at recruit ([`CommunionService.TryRecruit`]), read on Awake, carried through the Communion Totem's `m_customData` (`DE_TotemId` → `SpawnRecruited(..., companionId)`); an Elo helper (`src/Ranking/Rating.cs`, start 1000, K-factor + floor); a **server-authoritative `LeaderboardStore`** persisting one **JSON file per world** (`<save>/LostScrollsII/ladder.<world>.json`, via `UnityEngine.JsonUtility` — added the `UnityEngine.JSONSerializeModule` reference) with per-pair rating cooldown (anti friend-farm), W/L/streak/rating records keyed by companionId + denormalized owner id/name/companion-name/caste, and a `SeasonReset` that archives + bumps season; a **`LeaderboardSync`** RPC layer mirroring `GuidanceSync` (client→server `LSII_ReportDuel`; server→all `LSII_LadderPush`; client→server `LSII_LadderReq` on spawn; server→winner `LSII_RankEvent`), auto-bound via Harmony `ZNet.Awake`/`OnDestroy`/`RPC_PeerInfo`/`Player.OnSpawned` patches (picked up by `PatchAll`). Wiring: `DvergrCompanion.AwardDuelWin` now also reports the bout to the ladder (different-owner, non-zero, stable-id gated; loser id read from its replicated ZDO so no ownership steal); the server applies Elo, persists, broadcasts, and on a top-3 climb sends a rank event back to the **winner's client**, which fires the new **`dvergr_rank_changed`** ServerGuide trigger (`ServerGuideBridge.RaiseRankChanged`; matched in ServerGuide's dispatcher with a `caste:` filter + `{rank}`/`{rating}`/`{companionName}`/`{ownerName}`/`{caste}` templating) so the reward lands on the right player. Display: **`de_ladder [caste] [count]`** + admin **`de_season_reset`** console commands, an optional blue **`#rank`** on the companion floating name (config `Ranking.ShowRankOnNameTag`, alongside the `★level` badge), and a ServerGuide **`guidance.rankings.yaml`** (`category: Rankings`: a ladder reference page on first duel win + an "Among the Champions" top-3 milestone with buff/chat rewards) deployed to the ServerGuide `examples/`, the Quest pack, and the live test profile. New config section **`Ranking`** (`EloKFactor` 32, `PairCooldownSeconds` 300, `ShowRankOnNameTag` on). **Unverified — needs a 2-player session** ([docs/Testing.md](docs/Testing.md) §17); a rank==1 reward filter and dedicated-server rank-event path are noted open items. Party (Phase C/D) and tournaments (Phase E) remain planned per the agreed build order. **Competitive suite — Phase C (party duels) built (2026-07-09):** team-vs-team companion sparring, built on the verified 1v1 duel spine and building clean. A new **`DE_PartyDuel`** ZDO flag (mirrors `DE_Duel`: replicated, spawn-cleared, mutually exclusive with 1v1 duel) + a `PartyDuelMode` property and `InAnyDuelMode` helper on `DvergrCompanion`. Targeting: the `CompanionIsEnemyPatch` was generalized so a duelist is an enemy only to another player's duelist **in the same mode** (both `DuelMode` or both `PartyDuelMode`) with different non-zero owners — one rule that scales 1v1 and N-vs-M and keeps duelists immune to/ignoring players, creatures, and same-owner allies. Non-lethal: the `CompanionDamagePatch` floor now branches — 1v1 credits the striker via `ResolveSubdue`, party **benches** the member via `ResolvePartySubdue` (reusing the same idempotent `_duelResolved` latch so a member is benched exactly once) and the match plays on. Win **by attrition**: an authority-gated `TickPartyDuel` (per-second, like `TickDuel`) checks the owner leash (>40 m / logout → forfeit), locks the nearest enemy-team member when idle, and — once engaged — stands a surviving member down as a **winner** when no enemy-team party companion remains anywhere loaded, paying **team-size-scaled XP** (`base * loserPeak / winnerSize`, so winning while outnumbered pays more and winning by outnumbering pays less per head). Input: a new **`K`** key (`Duels/PartyDuelKey`, `MaxPartySize` default 4) gathers the player's nearby free Follow-stance companions into a team (nearest-first, capped) and toggles the whole team down; owner-gated, typing-suppressed, and a `[K]` hover hint added. **Phase D (party ranking) is NOT yet wired** — party bouts don't touch the ladder yet. Builds clean; **unverified — needs a 2-player session** ([docs/Testing.md](docs/Testing.md) §18). **Competitive suite — Phase D (party ranking) built (2026-07-09):** party bouts now feed a persistent ladder on the same `LeaderboardStore`/`LeaderboardSync` spine as 1v1. New `PartyRecord` keyed by **ownerId** (the "owner" half) with a **`memberSnapshot`** of companion id/caste/level (the "companions" half), team Elo (owner-vs-owner, start 1000), W/L, `bestTeamSize`, season; stored in the same per-world JSON (`parties` list, schema present since Phase A). Reporting: each surviving winner runs `AwardPartyWin`, but a **static owner-pair latch** (`s_partyReportLatch`, 15 s window) dedups so the ladder report + ServerGuide event fire **once per match** — every winner shares the same owner/authority client, so the latch is reliable without server round-trips. Rosters are **accumulated over the bout** (`ScanParty` fills ally/enemy `companionId→caste:level` dicts + captures the opponent owner each tick), so the winner can report BOTH teams even though the losers are benched/gone by stand-down. Server path: `LSII_ReportParty` → `LeaderboardStore.ApplyPartyDuel` (team Elo, member snapshots, best-team-size) → broadcast table → on a top-3 climb, `LSII_PartyRankEvt` back to the winner client fires **`dvergr_party_rank_changed`**; the win itself fires **`dvergr_party_duel_won`** locally on the winner (like 1v1's `RaiseDuelWon`), carrying `winSize`/`opponentOwner`/`mvpCaste`. Both new triggers matched in ServerGuide's dispatcher (type-only, no caste filter) with `{winSize}`/`{opponentOwner}`/`{mvpCaste}` templating added. Display: **`de_party_ladder`** command + party pages in `guidance.rankings.yaml` (reference on first party win, a per-win notice, and a top-3 party milestone with buff/chat) redeployed to examples/Quest pack/test profile. Builds clean; **unverified — needs a 2-player (ideally 2v2+) session** ([docs/Testing.md](docs/Testing.md) §19). Tournaments (Phase E) remain planned. **Competitive suite — Phase E (tournaments) built (2026-07-09):** a server-authoritative bracket runner for **both** 1v1 and party, reusing the existing duel modes and building clean (both projects). `TournamentService` holds a persisted `TournamentState` (JSON per world, `tournament.<world>.json`, resumed on `ZNet.Awake`) through phases idle→registration→running→complete; **re-seeded single elimination** (survivors re-sorted by ladder rating each round, highest-vs-lowest, top seed byes on odd counts). Crucially it **does not remote-drive combat** — the server announces each pairing and **resolves the match from the same `LSII_ReportDuel`/`LSII_ReportParty` report the ladders already receive** (`TournamentService.NotifyDuelResult`/`NotifyPartyResult` hooked in `LeaderboardSync`), so tournament fights are ordinary player-run duels. Commands: **`de_tournament start <1v1|party> [size]` / `join` / `begin` / `bracket` / `forfeit <name>` / `cancel`** (+ `de_champions`); admin subcommands are **host/server-gated** (no client→server admin RPC yet), `join` (client) registers the hovered companion (1v1) or the player's party owner (party) via `LSII_TourJoin` and seeds from the ladder rating. State syncs to clients (`LSII_TourPush` on join/spawn/change). Three ServerGuide triggers — **`dvergr_tournament_joined`** (join ack), **`dvergr_tournament_match`** (pairing, `{round}`/`{opponent}`), **`dvergr_tournament_won`** (champion, `{mode}`/`{bracketSize}`) — are fired on the **relevant player's client** (match/won broadcast with the target name embedded; each client acts only on its own) so ServerGuide rewards land on the right player; matched in the dispatcher (optional caste filter) with the new templating vars. `guidance.tournaments.yaml` (`category: Tournaments`) carries the join confirmation, the pairing notice, and the **champion prize bundle** (intro beat + buff + coins + optional title + Discord), deployed to examples/Quest pack/test profile. Champions are archived to a **Hall of Champions** (`champions.<world>.json`, `de_champions`). **Deviations from the doc, for robustness:** matches are player-run (no server teleport/auto-start), the **Arena Ward + teleport are not enforced** (convention only), admin subcommands are host-only. Builds clean; **unverified — needs a multi-player (ideally 4-entrant) session** ([docs/Testing.md](docs/Testing.md) §20). This completes the planned four-part competitive suite (Phases A–E); all of it remains pending live verification. **Map + compatibility batch, verified & released as 0.3.0 (2026-07-13):** (1) companion **map pins** switched from the 0-4 `MapPinIcon` sprites to the vanilla **player icon**, tinted + scaled down (`CompanionMapPins.StylePin`; config `CompanionPinColor` `FFB84D` / `CompanionPinScale` 0.7) so allies read as small players distinct from the owner's marker; (2) a per-companion **death marker** — a persistent named skull pin (`PinType.Death`, `save = true`) dropped on the owner's map wherever a companion dies, with or without items, via `CompanionMapPins.AddDeathMarker` hooked in the `Character.OnDeath` patch (owner-gated, client-side; config `ShowDeathMarker`); (3) the **BiomeLords container-UI conflict** fixed for real — `CompanionInventoryGui` routes its ComfyQuickSlots panel-shift through one `ShouldAdjustContainer()` gate that defers entirely to BiomeLords when detected (`com.taeguk.BiomeLords`) *and* honors a new manual off-switch `AdjustContainerPanel`, with a one-time `[inventory] container-panel shift: ON/OFF` diagnostic log. **All three verified in-game (2026-07-13)** — see [docs/Testing.md](docs/Testing.md) §14/§14b/§16e3. Packaging: version bumped **0.2.0 → 0.3.0** (csproj/Plugin/both manifests + the Quest pack's `TaegukGaming-Lost_Scrolls_II-0.3.0` dep), both package `CHANGELOG`/`README` updated, the Quest pack now bundles `guidance.rankings.yaml` + `guidance.tournaments.yaml` (with a noted caveat that their template vars need a matching ServerGuide republish), and both Thunderstore zips rebuilt at 0.3.0. The competitive suite (Phases A–E) ships in this build but stays **unverified pending multiplayer testing**. **Competitive UI + escrow tournaments + Discord + party naming (2026-07-17, unverified):** added a player-facing UI and reworked tournament entry across the whole suite (builds clean, both projects). (1) A **ranking board** (`F6`, `RankingBoard`) renders the duel + party ladders in a read-only vanilla `TextViewer` rune panel; `de_ladder`/`de_party_ladder` remain. (2) A **tournament panel** (`F7`, `TournamentRegistration`) is a self-contained cloned-widget Canvas with status/entrants + controls (**Lock Totem → Enter / Withdraw / View Bracket**, admin **Start / Begin / Activate Round / Cancel**); the read-only `TournamentBoard` (TextViewer) backs "View Bracket". (3) **Escrow tournaments (escrow & auto-summon):** you enter by locking a companion's **Communion Totem** into a slot — it leaves the inventory and is stored as a serialized payload on the `TournamentEntrant` (`totemPayload` / `teamPayloads`, serialized via new `TotemConversionService.SerializePayload`/`BuildTotemFromPayload`). Admin **Activate Round** (`TournamentService.ActivateCurrentRound`) broadcasts a summon (owner-id-filtered `LSII_TourSummon`) so each owner's client rebuilds + **summons** its companion(s) beside itself in duel mode (`TournamentClient.SummonForMatch`, tagged `TournamentCombatant`) against the **assigned bracket opponent** — new transient `DvergrCompanion.DuelOpponentId`/`DuelOpponentOwner` + a `MatchesDuelAssignment` gate added to `CompanionIsEnemyPatch` so simultaneous matches don't cross-target. On resolve (the existing duel-report path sets the match winner) the `TournamentClient` 1 Hz tick **reseals** the live companion(s) (updated XP/level) via `LSII_TourReseal` → `TournamentService.UpdateEscrow` and **despawns** them (`DvergrCompanion.DespawnToTotem`); totems are **returned** to owners on reject/withdraw/**admin release**/cancel/complete (`ReturnEscrowToOwners` → owner-filtered `LSII_TourReturn` rebuilds the item, dropping at feet if the bag is full). Console `de_tournament` now routes `join` through escrow (auto-seals the hovered companion / nearby Follow allies) and adds `withdraw` / `activate` / `release <name>`. (4) A new **admin-authenticated RPC** `LSII_AdminCmd` (server re-verifies `ZNet.IsAdmin` via the peer host name, mirroring ServerGuide's `GuidanceSync` admin RPCs; listen host runs directly) backs the admin UI/console controls from remote clients. (5) **Discord announcements** routed through ServerGuide: every **duel win** (1v1 + party, tournament matches included), every **new #1** on either ladder (new dedicated `dvergr_rank_first` / `dvergr_party_rank_first` triggers fired from `LeaderboardSync` when `winnerRank == 1`, so no numeric filter is needed), and the **tournament champion** now post to the server webhook via `type: discord` rewards in the new `guidance.duels.yaml` + updated `guidance.rankings.yaml` / `guidance.tournaments.yaml` (requires `DiscordWebhookUrl` set server-side). Enabler: a **ServerGuide reward-templating fix** — `RewardDispatcher.Grant` now takes an optional token expander so `chat_message`/`discord` reward messages expand the firing entry's full vars (`{companionName}`/`{rank}`/`{winSize}`/`{mode}`/…), not just `{player_name}`; `dvergr_duel_won` was enriched with `{companionName}`/`{opponent}`. (6) **Party naming:** `de_party_name <name>` (or the party registration path) names your party — stored in the persistent server-authoritative `PartyRecord.partyName` (new `LeaderboardStore.SetPartyName` + `LSII_SetPartyName` RPC), threaded through `PartyDuelResult.WinnerPartyName` and shown on the party ladder, the `F6` board, and party/rank announcements (`{partyName}`). New config: `Ranking/RankingUiKey` (`F6`), `Tournaments/TournamentUiKey` (`F7`). While the `F7` panel is open it **takes input over like a vanilla menu** — a `Player.TakeInput` postfix (→ false) blocks player controls, a `Player.SetMouseLook` prefix (→ skip) freezes the camera (mouse-look isn't fully gated by `TakeInput` in this build), and a `GameCamera.UpdateMouseCapture` prefix frees the mouse cursor for clicking (all restored on close; our F7/Escape and the uGUI buttons bypass these). The panel's **admin controls are admin/host-only** (`ZNet.LocalPlayerIsAdminOrHost`, server re-verified) and cover every setup command as buttons — Start 1v1 / Start Party / Begin / Activate Round / Cancel, plus per-entrant **Forfeit** and **Release**. **All of this is unverified in a live session** (needs 2 players, 4 for a full bracket, and a configured Discord webhook) — see [docs/Testing.md](docs/Testing.md) §21. **Post-batch input fixes (each from an in-game report):** the `F7` panel needed three more gates before it behaved like a real menu — `Player.SetMouseLook` skipped (the **camera** still rotated; `TakeInput` alone doesn't gate mouse-look in this build) and `ZInput.GetButton`/`GetButtonDown`/`GetButtonUp` swallowed (clicking a button also **attacked**; those read straight from `ZInput`), on top of `TakeInput`→false and the `UpdateMouseCapture` cursor release. Also, the panel's **admin controls were invisible to a real admin on a dedicated server** — `ZNet.LocalPlayerIsAdminOrHost()` is unreliable on a pure client, so the panel now asks the **server** (`LSII_AdminChk` → authoritative `ZNet.IsAdmin`), caches it in `LeaderboardSync.LocalIsAdmin` (requested on spawn + on open, panel rebuilds when the reply lands), with the listen host short-circuiting to true. **Released as 0.4.0 (2026-07-19):** version bumped across csproj/Plugin/both manifests, both package CHANGELOGs written, the Quest pack now bundles `guidance.duels.yaml` (5 guidance files) and depends on `TaegukGaming-Lost_Scrolls_II-0.4.0` + `TaegukGaming-ValheimServerGuide-0.9.0`. **Two upload zips are built and verified** (root-level manifest/icon/README, Quest keeps its `config/` subtree): `Lost_Scrolls_II_0.4.0.zip` and `Lost_Scrolls_II_Quest_0.4.0.zip`. **ServerGuide's zip was deliberately NOT cut** — that project is at 0.9.0 with **concurrent unfinished work in its tree** (a new `src/Display/RunePanel.cs` + `GuidanceDisplay`/`GuidanceConfig` edits) alongside our reward-templating fix and the two `*_rank_first` triggers, so packaging it here would ship someone else's half-finished feature; cut ServerGuide 0.9.0 from that project when its own work is ready. **Upload order: ServerGuide 0.9.0 → base 0.4.0 → Quest 0.4.0** ([docs/Publishing.md](docs/Publishing.md)). **Packaging/doc pass (same cut):** the Quest pack's guidance moved into a per-mod subfolder — `config/ValheimServerGuide/`**`LostScrollsII/`** — matching the convention already live on the dedicated server (`BiomeLords/`, `LivingWorld/`, `InterServerPortal/`); this rides on ServerGuide's **recursive** config loading (0.8.0+), and the deployed test copies were moved to match, with an **upgrade warning** (delete the old flat 0.3.0 copies or ids load twice) in the Quest README/CHANGELOG + Publishing.md. Both package READMEs/CHANGELOGs now state plainly that **rankings/tournaments require ValheimServerGuide 0.9.0+** to announce or reward anything (the ladders still record and `F6`/`de_ladder` still read without it) — the Quest README had been two versions stale (2 of 5 guidance files, dep 0.7.1, version footer 0.2.0). New player-facing wiki pages **`wiki/Rankings.md`** + **`wiki/Tournaments.md`** (the competitive suite had none), linked from `Home.md` (whose "only one panel" and "works fully without ServerGuide" claims were corrected). [docs/ServerGuide-Integration.md](docs/ServerGuide-Integration.md) now documents all eight competitive triggers with subjects/vars, why `*_rank_first` exists instead of a numeric `rank:` filter, and the 0.9.0 reward-templating fix. Packaged but still pending live verification. **Tournament batch + serializer fix — released 0.5.0 (2026-07-24):** a tournament feature batch on top of the escrow system, plus fixes — one of which was masking the whole tournament system on the dedicated server. Features: **three elimination formats** (`TournamentState.eliminationType` = `single`/`double`/`round_robin`; `Start` validates, `ResolveMatch` dispatches to `ResolveSingleMatch`/`ResolveRoundRobinMatch` [`BuildRoundRobinSchedule` circle method, champion = most wins]/`ResolveDoubleMatch` [loss-counted, eliminate at 2, pools recomputed each round] — **simplified**: DE grand final is single-game with no bracket reset, DE seeding for non-power-of-two is approximate, round-robin tiebreak is seed-then-name), picked on the **F7 panel via a Type cycle button** (or the start command's 4th arg); **level-gated entry** (config `Tournaments/RequiredEntrantLevel`, 0=off; totem level threaded `LockTotem`→`SendTournamentJoinEscrow`→`Join`, 1v1 only); **companion name+level on the bracket** (`TournamentEntrant.level` + `TournamentMatch.aLevel/bLevel`); **live W–L standings** (`TournamentBoard.AppendStandings`, sorted per format; single-elim losers now increment `losses`); **entrant cap** (config `Tournaments/MaxEntrants` default 4; `Start` clamps, size≤0 means "use the cap"); **full heal on summon** (`TournamentClient.SummonForMatch` → `Character.Heal(GetMaxHealth(), true)`). Fixes (the first three **verified against the dedicated server** this session): (1) **`UnityEngine.JsonUtility` silently drops `List<[Serializable] class>` fields** on Valheim's current Unity 6 runtime — so both the per-world save file AND the server→client snapshot lost the entrant/match and ladder-record lists, making the tournament show **0 entrants on every client forever** (and not survive a restart), with the ladders at the same risk; diagnosed by a boot-time round-trip self-test (`jsonHasLists=False`) and fixed with a hand-rolled [src/Ranking/CompetitiveJson.cs](src/Ranking/CompetitiveJson.cs) writer/parser, every `JsonUtility` call site in `TournamentService`/`LeaderboardStore` swapped over, self-test kept as a boot guard (now `jsonHasLists=True, entrants=1, matches=1`); (2) **F7 panel relog NRE** — the panel clones `InventoryGui` widgets but `InventoryGui` is rebuilt each world load while the panel root is `DontDestroyOnLoad`, so post-relog the clone sources were destroyed objects; `EnsureBuilt` now detects stale sources and rebuilds, with null-guards in `AddButton`/`Rebuild`; (3) **attack/movement leaked through the open panel** — the real gate is `PlayerController.TakeInput()` (private, distinct from `Player.TakeInput()`) and a competing mod (Valcoin) was out-ordering the `ZInput` swallow; fixed with a **postfix on `PlayerController.TakeInput`→false** while the panel is open (can't be out-ordered); (4) **View Bracket** now calls `RequestTournament()` before rendering so it can't show a stale snapshot. Guidance (Quest pack): `guidance.rankings.yaml` + `guidance.tournaments.yaml` rewritten to be **UI-driven** (F6/F7), **all console commands removed**, new *"Entering a Tournament"* how-to page; synced to ServerGuide `examples/`, Quest pack, dedicated server, test profile. **No ServerGuide release needed** (guidance uses only existing 0.9.0 triggers/templating). Bumped to **0.5.0** (csproj/Plugin/both manifests + Quest dep `TaegukGaming-Lost_Scrolls_II-0.5.0`); both package CHANGELOG/README updated; both zips rebuilt. Upload order: base 0.5.0 → Quest 0.5.0. Tournament-flow items still need a live multi-player pass ([docs/Testing.md](docs/Testing.md) §21). **Communion Rite reworked into a channeled struggle — released 0.6.0 (2026-07-27, base-mod only, unverified):** recruiting is no longer an instant keypress. A new `CommunionRite` MonoBehaviour (`src/Companions/CommunionRite.cs`) drives a **channeled rite**: begin it by **holding the vanilla Block button** (`ZInput.GetButton("Block")`, keyed off *held* not the press down-edge, since players block continuously through a fight and the down-edge is missed) with the crosshair on a subdued Dvergr (≤20% HP), and hold for `CommunionChannelSeconds` (default 5 s). We only *read* the input, so the shield still raises and **blocking + dodging keep working** through the rite (dodge shares the Block button; a `ReleaseGraceSeconds` ≈ 0.5 s grace forgives the brief release a roll causes). It **can fail** — release Block past the grace, stray past `CommunionMaxDistance` (4 m), take damage (`CommunionBreakOnDamage` on; a *blocked* hit deals none), or the target dies/unloads → the shadow reclaims it (re-aggravates, must be re-subdued). Success runs the existing `CommunionService.TryRecruit` (+ one small `PlaySummonVfx` poof). `G`/`CommunionKey` is now **Feed-only** (`HandleCommunionInput` split into `HandleFeedInput` + `TryBeginCommune`). The **only ambient effect is an accelerating Wishbone ping** — the vanilla ripple `SE_Finder.m_pingEffectNear` played *directly* (not via the status effect, which only pulses near buried treasure so it showed nothing) on **both the Dvergr and the player**, its interval lerping 1.0 s → 0.28 s as the rite nears completion (the quickening ping is the progress cue). An earlier floating-name/hover progress bar and per-lash smoke VFX were tried then **removed at the user's request** (the discarded bar switched from Unicode `▮/▯` — empty boxes in the serif font — to ASCII `[|||---]` before removal). Also fixed a latent `Fail()` bug that read `_player` after `Reset()` nulled it. New config (`Recruitment`): `CommunionChannelSeconds`, `CommunionMaxDistance`, `CommunionBreakOnDamage`. MP lock via `DE_Communing` ZDO. ServerGuide untouched (stays 0.9.0); Quest pack guidance unchanged. Builds clean; **unverified in a live session.** ([docs/Ally-Recruitment.md](docs/Ally-Recruitment.md), [docs/Changelog.md](docs/Changelog.md).) **Movable chest/storage UI (2026-08-03, unverified):** the shared container panel (`InventoryGui.m_container` — vanilla chests *and* the companion pack) is now **placed by config and moved by dragging**, replacing the old CQS row-counting shift that inferred what other slot mods had done. New [`ContainerPanelPositioner`](src/Companions/ContainerPanelPositioner.cs) (component on the plugin GameObject): a transparent `Image` stretched over the panel as its **first child** is the grab surface — every real widget (item slots, Take All, our name field) is a later sibling so it stays in front and keeps its own clicks, leaving the panel's empty background draggable (faint white wash on hover; `ContainerDragSurface` handles the pointer events). Dropping the panel writes an `"x,y"` pixel offset to the new config **`Interface/ContainerPanelOffset`** (BepInEx persists it; re-read live when the value changes); `"auto"` = **two inventory rows below** the vanilla spot (`2 × m_playerGrid.m_elementSpace`, measured live) so mod-added inventory rows aren't hidden; `de_container_reset` restores it. `ClampToScreen` keeps ≥48 px on screen so a drag or a stale cross-resolution config can't strand the panel. **BiomeLords ships the same feature for the same panel, so when it's loaded the whole system disables itself** (no offset, no drag surface) — manual off-switch `Interface/MoveContainerPanel = false`; logged once as `[inventory] container-panel positioning: ON/OFF (config=…, BiomeLords=…)`. Removed: `Companions/AdjustContainerPanel`, the row-counting shift, and the BiomeLords check inside `CompanionInventoryGui` (which no longer touches panel placement). Builds clean; **unverified in a live session** ([docs/Testing.md](docs/Testing.md) §16e2/§16e3, [docs/Ally-Inventory.md](docs/Ally-Inventory.md)). **Released as 0.7.0 (2026-08-03):** version bumped across csproj/Plugin/base manifest; the **Quest pack was already at 0.7.0** for the uncommitted **Bog Witch rites** batch (`guidance.bogwitch-rite.yaml` — a weekly Bog Witch quest chain that spawns a wild Dverger/DvergerMage in the Swamp so players can get their first companions without the Mistlands; ServerGuide-content-only, soft-dependent on `ProfMags-TraderOverhaul`), so both packages now line up at 0.7.0 and the Quest dep moved to `TaegukGaming-Lost_Scrolls_II-0.7.0`. **ServerGuide untouched (stays 0.9.0)** — upload order is base 0.7.0 → Quest 0.7.0. Both zips rebuilt (`Lost_Scrolls_II_0.7.0.zip`, `Lost_Scrolls_II_Quest_0.7.0.zip`); GitHub repo + wiki pushed ([docs/Publishing.md](docs/Publishing.md)). **Bounty hunting — built A–I and released as 0.8.0 (2026-08-20/24, gameplay verified in-game):** a **server-only, triple-gated** endgame loop ([docs/Bounty-Hunting.md](docs/Bounty-Hunting.md), [Testing.md](docs/Testing.md) §23–§23h all ✅). It runs **only** where **BiomeLords + ValheimServerGuide + Valheim Donations** are all loaded **and** the instance is a server/host (`BountyFeatureGate`, one early-out; all source isolated under `src/Bounty/`); clients are *told* whether it's live over `BountySync` because they can't see the server's plugin set, and a client on a non-qualifying server (or single-player) still gets the `F8` panel + inventory button showing a **teaser** explaining where it runs. **Targets:** ordinary vanilla Dvergr prefabs made **hostile on sight** (`BountyTarget` — `SetAggravated`+`SetHuntPlayer`, alert range widened, re-asserted on a 3 s tick, `m_randomMoveRange` tightened so they stay near their pin), with a tier-scaled escort. **Our own 5-tier curve** (`BountyTiers`) — BiomeLords has **no callable scaling API and no minion scaling at all**, so both are ours; **health is scaled explicitly, damage rides on the vanilla star level** (vanilla already scales creature damage per star, so patching the attack path would buy nothing), star level capped at 3 and spread 1/2/2/3/3 across tiers. Because vanilla renders level 1 as **zero** stars and `EnemyHud` has only two star rects, tiers also wear a **text badge** (`[T4 Dread Bounty]` / `[Escort]`, `BountyNameBadgePatch`) — the same wall the companion `★N` badge hit. **Placement** (`BountyLocationSampler`) reads `WorldGenerator` (procedural, answers for unloaded zones — a raycast only works near a loaded player) and validates **three rings out to 80 m**, because a single ring passes on any islet wider than twice its radius; BlackForest/Swamp/Mountain/Plains only. **A posting is a location + tier, not a live creature** — the camp spawns from the hunter's own client on arrival (~80 m), since Valheim doesn't simulate unloaded zones; the client reports the spawn so a relog mid-approach can't stack a duplicate. **Requirement 1 holds:** a bounty target can be **communed** exactly like any Dvergr, and that closes the bounty and pays the same as killing it (`BountyTarget` stands down the moment a `DvergrCompanion` appears). **Rewards contain no loot table in C#** — resolution fires `dvergr_bounty_resolved` carrying the tier and `guidance.bounty-rewards.yaml` decides what it's worth (same split as ranking/tournaments); payout is idempotent via a resolved-id set. **Valcoin is reward-only by construction** — the chance is rolled in-mod (ServerGuide has no numeric rank filter and no random reward) from the player's best duel/party standing, and only on success does `dvergr_bounty_valcoin` set the `VC.Q.ls_bounty_t<N>` key Valheim Donations reads; **no coin amount ever leaves this mod**, satisfying that project's documented "no selling power" guardrail. **Two distinct rank levers:** bounty-ladder standing bumps the **reward tier** (`BountyLeaderboardStore`, owner-keyed, **tier-weighted points not Elo** since bounties aren't head-to-head); duel/party rank drives the **Valcoin chance** *and* gates the **Accursed (tier 5)** postings — access is the stronger pull toward duels, which is the point of the feature. **Board** (`BountyBoardStore`, `board.<world>.json`): server-authoritative postings, accept/abandon decided server-side, **rotation** retires *unclaimed* postings after `RefreshHours` (accepted ones and the commission never expire), one elite posting open at a time shown **locked-but-visible** to unqualified players. **Quest gate** (`BountyQuestGate`) needs **no new API in either mod**: Haldor's `npc_conversation` (hold `E`) grants a stock `set_player_key` (`LS_BountyStart`) that the mod watches for and clears, and answering that commission sets `LS_BountyUnlocked` — an event the mod already observes **because it posted the bounty itself**, which is why ServerGuide's missing reverse-query API is a non-issue rather than a blocker. Both are Valheim unique keys (persist per character, no storage of ours). **`ProfMags-HaldorBounties` is being retired from the server in favour of this**, so Haldor is free to use. **Serializer discipline:** every new store uses `CompetitiveJson` from day one, never `JsonUtility` (which silently drops list fields on this runtime — the bug that once made tournaments show 0 entrants); float support added with **`InvariantCulture`** since a comma-decimal locale would corrupt every coordinate. UI: `F8` panel + a **Bounty Board button at the top-centre of the inventory screen** (placed deliberately with `LayoutElement.ignoreLayout` — the container's layout group was overriding it), sharing the F7 panel's input-capture patches via a new `ModalPanels.AnyOpen` gate rather than duplicating them (they encode two live-server fixes: `PlayerController.TakeInput` ≠ `Player.TakeInput`, and the ZInput gate must be a postfix because Valcoin out-orders a prefix). Fixes made from live reports: invisible tier stars (above), and **season resets refused a remote admin** — both `de_bounty_season_reset` **and** the pre-existing `de_season_reset` gated on *being* the server rather than on *being an admin*, now routed through the admin-authenticated `LSII_AdminCmd` RPC. **Released 0.8.0** (csproj/Plugin/both manifests; Quest pack bundles **8** guidance files; new `wiki/Bounty-Hunting.md`); **ServerGuide 0.14.0 cut alongside** carrying the `tier:` filter + the two `dvergr_bounty_*` triggers + 4 tokens (plus two of its own NPC-dialogue fixes). **Upload order: ServerGuide 0.14.0 → base 0.8.0 → Quest 0.8.0**; the Quest dep on ServerGuide is pinned **hard** because on an older build `MatchesTrigger`'s `default: return true` ignores the `tier:` filter and **every** tier's bundle would fire at once. Server-side setup still pending: `ls_bounty_t1`…`ls_bounty_t5` in the donations mod's `valcoin_quests.yaml` (item rewards pay regardless). See [docs/Publishing.md](docs/Publishing.md). **Field sealing + wagered tournaments & duel invites (2026-08-23, unverified):** three additions, one reaching outside this mod ([docs/Wagers.md](docs/Wagers.md), [docs/Testing.md](docs/Testing.md) §24–§26). (1) **Dead Raiser sealing** — a portable counterpart to the Incinerator ritual: equip a `StaffSkeleton`, hold a **Wisp**, have **Blood Magic 20+**, and hold **Block** on your own **Follow-stance** companion to seal it into a Communion Totem in the field. The channel scales **5 s at skill 20 → 2 s at 100** and breaks on the Communion Rite's conditions (release past a dodge-forgiving grace, distance, damage) plus its own (staff unequipped, wisp gone). Built as a **sibling** of `CommunionRite`, not a mode inside it — they share only the held-Block idiom and the accelerating Wishbone ping; each helper early-outs on the other's target (unrecruited subdued Dvergr → commune, your own companion → seal) so they can never both fire, and the seal path is **silent with no staff equipped** so blocking beside an ally never nags. The totem comes from the same `TotemConversionService.CreateTotem`, so both paths produce identical items; a full pack drops it at the player's feet rather than destroying it. New `Recruitment` config keys (`StaffSealEnabled`, `SealMinBloodMagic` 20, `SealFullSpeedBloodMagic` 100, `SealChannelMaxSeconds` 5, `SealChannelMinSeconds` 2, `SealMaxDistance`). (2) **Player-started staked tournaments** — anyone opens one for **100 Coins / 10 Valcoins** (which *also* pays their own entry, never charged twice), fixed **4-player** single elim that **begins automatically** when full, champion takes **999 Coins / 100 Valcoins**; **one Coin and one Valcoin tournament at a time**, enforced by turning `TournamentService`'s single state into a **book of slots** (`""` free / `"coins"` / `"valcoin"`) — every method now takes a slot key, and an older `tournament.<world>.json` still loads into the free slot. The design point is **ready-up**: the free admin tournament summons companions wherever their owners stand, which is wrong for a player-run event, so a wagered pairing carries `aReady`/`bReady` and is summoned **only when both owners press Ready**, wherever they chose to meet — that is what "the players decide the location" means, and it's stated on the panel, the board and the bracket announcement. Unfilled/cancelled brackets refund **every stake and totem**; escrowed totems are returned on a **6 s completion grace** so the winner's reseal (carrying the XP it just gained) lands first. (3) **Staked duel invites** (`DuelInviteService`, `duelinvites.<world>.json`) — post for **100 Coins / 10 Valcoins** + a sealed companion, anyone accepts by matching, **winner takes both stakes**; **one invite per player at a time** (poster *or* challenger), any number of players at once. Deliberately its own flat two-sided record rather than a one-match tournament (no bracket/rounds/seeding, different lifetime); it reuses the escrow/summon/reseal plumbing via a **context tag** on `TournamentCombatant` and resolves off the **same duel report** the ladder receives. **Discord:** every server-wide beat (tournament opened, player entered, draw, duel started, duel won, who's next, champion, end-of-event summary, whole invite lifecycle) posts **straight to the ServerGuide webhook** via the public `DiscordAnnouncer.AnnounceRaw` (`ServerGuideBridge.AnnounceDiscord`, gated on `IsServer` so only the authority posts) — **no new trigger types, no new template vars, no ServerGuide release**; the rule established is *per-player outcomes → triggers+guidance, server-wide announcements → AnnounceRaw*. **The Valcoin problem:** this mod has never named a Valcoin amount (payouts ride a `VC.Q.<id>` key the donations mod prices), and there was **no debit path at all** — the ledger is backend-authoritative and a local `CoinManager` deduction is reverted by the next balance sync. So a small server-side API was added **to the donations plugin** (`ValcoinWallet.Charge`/`Credit`), wrapping endpoints that already existed: `/api/spend` for the debit (sku is regex- not catalog-validated, so `eco_ls_tourney_entry` works as-is) and `/api/admin/grant` for the credit (its own docstring names "event prizes, refunds"). LSII calls it **by reflection** (`src/Economy/ValcoinBridge.cs`) so donations stays optional — absent it, Valcoin wagers are refused with a reason and **Coin wagers keep working**. The guardrail holds where it matters: a duel purse is *exactly* the two stakes collected (a transfer, nothing minted); only the tournament purse exceeds the entries, and that surplus is **minted by the donations mod** from its own `valcoin_quests.yaml` (`ls_tournament_prize`, fired via the new `dvergr_tournament_prize` trigger) — `TournamentValcoinPrize` on this side is **display only**. Coins are inventory items so their stake is taken **client-side** and reported (same trust model as duel-result reporting), while Valcoins are debited **server-side and asynchronously** — which is why every wager entry point is callback-shaped, and why joins/posts re-check and refund if the event closed while a charge was in flight. New config section **`Wagers`**; new `guidance.wagers.yaml` (Quest pack); new `de_duel_invite` console command and a slot-aware `de_tournament`. **Server setup for Valcoin wagers only:** rebuild/deploy the donations plugin, add `ls_tournament_prize` to `valcoin_quests.yaml`. Builds clean (both projects); **entirely unverified in a live session** — needs 4 players for a full bracket and 2 for invites. **Inventory menu bar (2026-08-23, unverified):** every full-screen panel was reachable only by function key, which isn't discoverable — a player who never read the config or the wiki had no way to learn the ranking board existed. The lone Bounty Board button on the inventory screen became a **centred row of three** (Rankings `F6` / Tournaments `F7` / Bounty Board `F8`) in a new [`InventoryMenuBar`](src/Companions/InventoryMenuBar.cs), which **replaces `BountyInventoryButtonPatch`** (deleted) and generalises it: the buttons are a `MenuEntry[]` table and the row geometry is derived from the count, so a fourth panel is one line. Each is still a clone of the inventory's own Take All button (vanilla assets only), still `LayoutElement.ignoreLayout = true` so the container's layout group can't move it, still re-created on every `InventoryGui.Show` (InventoryGui is rebuilt each world load), still parented to the inventory screen ROOT so "centre" means the screen's centre, and still closes the inventory before opening a panel. The "already present?" probe is **per button**, so a partially-built row heals instead of staying broken. The function keys are unchanged. New config `Interface/MenuBarOffset` nudges the whole row; the old `Bounty/InventoryButtonOffset` is kept and **wins when it has been changed from `"0,0"`**, so an operator who had already dialled it in doesn't silently lose it. Builds clean; unverified ([docs/Testing.md](docs/Testing.md) §27). **Bounty gate fixed — released 0.9.1 (2026-08-25, verified):** a player reported completing Haldor's conversation and getting **no first bounty pinned**. Everything on this side was healthy (feature gate logged `ON`, board held 3 postings); the break was **in ServerGuide**. Root cause: the commission grants `set_player_key: LS_BountyStart` from a choice inside `conversation.nodes:`, those parse into **`NodeChoiceSpec`, which had no `Rewards` property at all**, and ServerGuide's loader runs `IgnoreUnmatchedProperties()` — so the block was discarded at parse time with **nothing in the log**; and even parsed, `OnNodeChoiceSelected` never called `RewardDispatcher`. Only the flat `conversation.choices:` path ever granted choice rewards, and `git log -S` showed the property absent since ServerGuide v0.1.0 — this had **never** worked through Haldor (§23g was presumably verified via the `de_bounty_commission` console tool). Ironically `guidance.bounty.yaml`'s own header comment diagnosed half of it and then attached the reward to the wrong class. **Fixed in ServerGuide 0.15.0** (`NodeChoiceSpec.Rewards` + a `GrantChoiceRewards` call placed *before* the `goto_node`/`goto` branch, latched once per open conversation so a looping tree can't be farmed; recorded as ServerGuide **invariant 20** — the two conversation shapes must stay at feature parity). **Second bug found while packaging:** ServerGuide's `npc_conversation` trigger moved hold-`E` → **`Shift + E`** in its 0.14.0, but `BountyBoardPanel`'s locked teaser, `guidance.bounty.yaml`, `wiki/Bounty-Hunting.md` and Testing.md all still said hold-`E` — only the dedicated server's copy had been hand-corrected. The board is the *only* in-game explanation of how to begin, so this was a dead end for new hunters. All corrected. **Packaging:** 0.9.0 was **already on Thunderstore** (2026-08-23 16:05 UTC) and versions can never be replaced, so the fix shipped as **0.9.1**; the Quest pack's ServerGuide dep is now a hard **0.15.0** pin (below that the commission silently grants nothing) and both README floors were raised. The **Quest pack had been stuck at 0.7.0 on Thunderstore**, two releases behind — anyone installing the complete pack was getting no bounty or wager content at all. Both 0.9.1 zips are built and verified; the LSII GitHub wiki was synced (it was missing `Bounty-Hunting` and `Wagers` entirely). See [docs/Publishing.md](docs/Publishing.md) and [docs/Changelog.md](docs/Changelog.md). **Totem persistence, the companion combat leash, and InterServerPortal travel (2026-08-30, VERIFIED 2026-08-31, released 0.10.0):** three reported bugs and one feature ([docs/Changelog.md](docs/Changelog.md), [docs/Testing.md](docs/Testing.md) §28–§30). (1) A **sealed companion reverted to a Fuling Totem on relog** — the per-instance `SharedData` clone was re-applied only on the two `ItemDrop.LoadFromZDO` overloads, but the player's inventory and every `Container` load through **`Inventory.Load`**, which rebuilds each item from its prefab. The name reset was the visible half; the returning **`m_maxStackSize` 20** was the dangerous half, since Valheim stacks by shared *name* and ignores `m_customData` — two sealed companions would have merged and one been lost. Fixed **before** the stacking decision, on a prefix of the private `Inventory.AddItem(ItemData,int,int,int)` (the first point where the item is both identifiable and still un-stacked), plus an `Inventory.Load` postfix as a catch-all. The class comment had pointed at a `GoblinTotemStackPatch` that was never written. (2–4) **Companions fought when they shouldn't**, all from one wrong assumption: the code set `MonsterAI.m_alertRange = 0` to make an ally passive, which **does nothing** — acquisition runs on `m_viewRange`/`m_hearRange`, and the one `m_alertRange` leash in `UpdateTarget` is gated on `IsTamed()`, which a freed Dvergr is not. The rule now lives in `DvergrCompanion.AllowsCombatTarget`, enforced at the two points vanilla splits the decision: a postfix on **`BaseAI.CanSenseTarget`** for acquisition (chosen over `BaseAI.IsEnemy`, which is symmetric and read by `HaveFriendInRange` — suppressing it would have made a passive Support mage **heal greydwarves**; `CanSenseTarget` is one-directional, so creatures can still attack a passive ally, which is how it gets provoked) and a per-frame target drop on the ZDO owner for retention (`UpdateTarget` keeps a locked target between throttled scans). **Follow** allies fight only within `Companions/FollowEngageRange` (default **20 m**, a workbench's build radius) of the master, measured both ways — they won't set off after something far from you and **break off a chase** the moment it drags them past the radius; a hostile **player** is exempt (the leash is about wildlife, not PvP). **Chore** and **Standby** allies acquire nothing at all and answer only what has actually hurt them — creature attackers are now marked hostile in `CompanionDamagePatch` (same-owner friendly fire excluded). **Standby also stops wandering** (`m_randomMoveRange` zeroed, collapsing `BaseAI`'s idle wander onto its own position; restored while it has a live target, since that field doubles as the charge-circling radius). The same tick re-asserts a Follow ally's follow target, so a **relogged** companion walks back on its own (vanilla doesn't persist `m_follow`). (5) **Companions travel through InterServerPortal's two extra modes.** Neither runs the vanilla teleport (that mod prefixes `TeleportWorld.Teleport` and returns false for flagged portals), so `CompanionPortalPatch` never saw them. **Network** mode ends in a plain `Player.TeleportTo`, so a postfix on `NetworkController.Travel` reuses the existing move-to-exit path (+ a prefix applying the wood-portal cargo block). **Inter-server** mode leaves the world entirely, and a companion is a ZDO in the world being left — the only thing that crosses is the character file — so the crossing **seals each follower into a Communion Totem** (prefix on `WorldSwitcher.Leave`, the commit point one line before the saving logout) and `InterServerArrival` summons them back on the far side; a full pack leaves the ally behind rather than dropping a totem in a world you're leaving. All of it is a **soft dependency**, patched by reflection and skipped when the mod is absent; every portal path now shares one `CompanionTeleport.Followers` definition of who travels. New config `Companions/FollowEngageRange`. **Build/deploy:** the client-side deploy targets were cut to the **one** Gale `HB Test` profile — the raw game-install `BepInEx\plugins\LostScrollsII` folder and both r2modman profiles are no longer written to and their stale copies were deleted (several builds of the same DLL across profiles made it impossible to tell which was under test); the dedicated-server target is unchanged. **Rest healing + leash tuning (2026-08-30, VERIFIED 2026-08-31, released 0.10.0):** the Follow combat leash default moved **10 m → 20 m** (a workbench's build radius; the live config files on both deploy roots were bumped too, since BepInEx keeps a persisted value over a changed default). And **resting at camp now mends Follow companions** — sit by a fire or stand under a roof with one lit and every Follow ally within `Companions/RestedHealRadius` (10 m) heals to full over `Companions/RestedHealSeconds` (120 s), with a vanilla **Resting** status icon above its health bar. The decision worth remembering is **which** status effect it keys off: vanilla has two and they're easy to confuse — **`Resting`** is the live state (added while near a fire and sitting/sheltered, removed the instant you stand up) while **`Rested`** is the lingering buff that survives 5+ minutes of travelling, so keying off `Rested` would have healed allies halfway across the map. Runs on the **owner's client** (`CompanionRestedHeal`, 2 s tick matching `BaseAI`'s regen cadence) because the rest state is computed locally and isn't replicated; healing from there is safe since `Character.Heal` routes to the ZDO owner over `RPC_Heal` and clamps to max HP. Chore/Guard/Standby/duel/feral allies are excluded. ([docs/Testing.md](docs/Testing.md) §31.) **Alert-bark fix (2026-08-30, VERIFIED 2026-08-31, released 0.10.0):** a report of "multiple voices when the Dvergr attacks" traced to `BaseAI.SetAlerted`, which is **edge-triggered** — on a false→true transition it spawns `m_alertedEffects` (the Dvergr's alert shout) and re-fires the `alert` animator bool. Vanilla writes `true` from places that run continuously (`MonsterAI.UpdateAI` every tick while it can see its target, `MonsterAI.OnDamaged` on **every hit taken**) and gets away with it because nothing clears the flag mid-fight. Two places here cleared it **every frame** — the new stance/leash target drop in `DvergrCompanion.Update`, and the pre-existing `ApplyEncumbrance` (which `CompanionInventoryAI` deliberately calls per frame while overloaded) — so an ally taking hits it wasn't allowed to answer (past the leash, on a chore, on Standby, or over the weight cap) shouted **once per incoming blow**. Both now route through a latched `DvergrCompanion.StandDown()`: always drop the target, clear the alerted flag at most once per stand-down, latch reset when it legitimately holds a target again / drops under the cap / on a deliberate stance change. Vanilla's own 30 s no-contact timeout finishes calming it. **Rule to keep: never write `SetAlerted` on a repeating tick** — it is an effect-spawning edge setter, not a state to re-assert ([docs/Testing.md](docs/Testing.md) §32). **Inter-server crossing crash fixed (2026-08-30, VERIFIED 2026-08-31, released 0.10.0):** a live attempt threw `InvalidOperationException: Collection was modified` from `CompanionTeleport.Followers`, the player never switched worlds, and a follower despawned then reappeared. `Followers` was a lazy `yield return` iterator over `DvergrCompanion.All` — a **static HashSet** maintained by the component's `OnEnable`/`OnDisable` — while the crossing sealed each follower as it walked it, and `DespawnToTotem()` destroys the GameObject, which removes the entry from the set being enumerated. It now returns a materialised `List<DvergrCompanion>` snapshot, so no caller can mutate the registry out from under it. The switch failing at all was the second half: the throw came from a Harmony **prefix**, and a prefix exception propagates instead of running the original, so `WorldSwitcher.Leave()` never called `Game.Logout` — our optional feature took the host mod's core flow down with it. All three bridge hooks are now try/catch-wrapped (a companion left behind beats a crossing that silently doesn't happen). The Dvergr reappearing was `InterServerArrival` correctly summoning the already-sealed companions in the same world; alongside it, the pending list is now timestamped as each entry is added rather than after the loop, so a half-finished seal can't leave `HasPending` true with no expiry. **Released 0.10.0 (2026-08-31):** all five of the above went through a live session first (§28–§32 all passed) and shipped together — version bumped across csproj/Plugin/both manifests + the Quest dep pin, both package CHANGELOG/README updated, both zips rebuilt (`Lost_Scrolls_II_0.10.0.zip`, `Lost_Scrolls_II_Quest_0.10.0.zip`), wiki updated (companion stance/leash, rest healing, InterServerPortal travel; also corrected a stale `MapPinIcon` reference removed back in 0.3.0). **No ServerGuide release and no Quest-pack content change** — nothing this batch touches guidance, triggers or templating, so the Quest pack is a dependency bump only and ServerGuide stays at the published 0.15.0. **Upload order: base 0.10.0 → Quest 0.10.0** ([docs/Publishing.md](docs/Publishing.md)). **Quest pack discontinued + 0.9.1 confirmed published (2026-08-31):** the owner confirmed **both 0.9.1 packages were uploaded**, and **retired the `Lost-Scrolls-II-Quest` package** — 0.9.1 is its last release (it stays installable on Thunderstore). It never shipped a DLL; all it did was bundle the nine guidance YAMLs and pull the base mod + ServerGuide in as dependencies. Those files were verified **byte-identical** to the `Valheim ServerGuide/examples/LostScrollsII/` source of truth and moved to a new repo folder **`guidance/`** with a README carrying the install steps and the ServerGuide **0.15.0** floor; players copy them into `BepInEx/config/ValheimServerGuide/LostScrollsII/` by hand. Folding them into the base package's own `config/` subtree (which Thunderstore would route to `BepInEx/config/`) was offered and **rejected** — the base package stays gameplay-only. The package folder was removed from `Thunderstore files/` (recoverable from git history) and the never-published `Lost_Scrolls_II_Quest_0.10.0.zip` deleted; the historical Quest zips of already-published releases are left alone. Every player-facing pointer — the base package README (which **ships inside the zip**, so the 0.10.0 zip was rebuilt after the edit), the repo README, and `wiki/Rankings.md` / `Tournaments.md` / `Bounty-Hunting.md` / `Recruiting-Companions.md` — now links `github.com/yesu0725/Lost-Scrolls-II/tree/main/guidance` instead of naming the pack; **moving or renaming that folder breaks links in a published package and on the wiki.** [docs/Publishing.md](docs/Publishing.md) is rewritten around a single package (zip commands, upload order, version-bump checklist) and records why. Upgrade hazard to keep repeating: installing the manual files **without** uninstalling Quest 0.9.1 first leaves two copies of every guidance id.
